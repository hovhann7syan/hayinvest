import createMiddleware from 'next-intl/middleware';

export default createMiddleware({
  // Список языков
  locales: ['hy', 'en', 'ru'],

  // Главный язык
  defaultLocale: 'hy',

  // 👇 ВАЖНО: Отключаем авто-определение языка браузера.
  // Теперь сайт будет открываться на армянском (hy), даже если у пользователя браузер на английском.
  localeDetection: false
});

export const config = {
  // Матчер оставляем стандартным
  matcher: ['/', '/(hy|en|ru)/:path*']
};